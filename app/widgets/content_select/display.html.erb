<%= select_tag widget_id, options_for_items(@items, @selected) %>

<script type="text/javascript">
	$("#<%= widget_id %>").change(function(event){
		value = $("#<%= widget_id %> option:selected").val();
		url = "<%= trigget(:container, :change) %>&value="  + value;
		$.getScript(url);
	});
</script>
