<%= widget_div do %>
<% if @my_cart.present? %>
	<div id="cart_box">
			<%#= form_tag cart_clear_list, :class => "clear_list" do %>
				<!-- <input type='submit' class='btn' value="<%#=t(:clear_list) %>" /> -->
			<%# end %>
		<div class="cart_btns">
			<input type='button' class='btn clear_list' value="清空列表" %>
			<%= link_to_if @my_cart.items.count > 0,
						t(:checkout),
						person_cart_index_path(current_user.login),
						:class => 'btn btn-primary checkout' do %>
			<%= link_to t(:checkout),
						person_cart_index_path(current_user.login),
						:class => 'btn btn-primary checkout disabled' %>
			<% end %>
		</div>
		<table class="cart_table">
			<tr>
				<td>图片</td>
				<td>标题</td>
				<td>数量</td>
				<td>价格</td>
			</tr>
			<%= render :partial => "cart/product_item", :collection => @my_cart.items %>
		</table>
		<div class="cart_bottom">
			<div class="product_total">总计：￥0</td>
		</div>
	</div>
<% end %>
<% end %>