<div class="row-msg">
  <div class="<%=message.send_user==current_user ? 'self' : 'people' %>">
    <div class='wrapper'>
      <div class="people-name">
        <p><%=message.send_user.login %>:</p>
      </div>
      <div class="msg">
        <p><%=message.content %></p>
      </div>
    </div>
  </div>
</div>