<% active_section :transactions %>

<div class="page-header">
  <h1>已下单 <small>你挑选的宝贝都在这里吗?</small></h1>
</div>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @people_transactions.each do |people_transaction| %>
  <tr>
    <td><%= link_to 'Show', people_transaction %></td>
    <td><%= link_to 'Edit', edit_people_transaction_path(people_transaction) %></td>
    <td><%= link_to 'Destroy', people_transaction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'New Transaction', new_people_transaction_path(@people) %>
