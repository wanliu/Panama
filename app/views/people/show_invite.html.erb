<div class="span8 widget">
    <p style='font-size: 14px;margin-bottom: 15px;margin-top: 15px;'>
        <%= form_tag "/people/#{params[:shop_name]}/#{params[:action]}", :method => "POST" do %>
            <%=@shop.name %> 商店邀请你加入他们，是否同意？<br /><br />
            <input type='hidden' name='auth' value="<%=params[:auth] %>" />
            <% if params[:login] %>
                <input type='hidden' name='login' value="<%=params[:login] %>" />
            <% end %>
            <input type='submit' class='btn btn-primary' value='同意' />
            <a class='btn' href='/'>拒绝</a>
        <% end %>
        <input type='submit' class='btn btn-primary' value='同意' />
        <a class='btn' href='/'>拒绝</a>
    <% end %>
    <p>
      谢谢！
    </p>
</div>