<div id="main" class="content">
	<section class="widget">
		<ul id="activity-category" class="nav nav-tabs activity-category" >
                <li class="active">
                    <a href='#' ><%=icon 'eye-open' %> </a>
                </li>
            </ul>
	 </section>
	<div class='category_products_panel'>
		<div id="category_products"></div>
		<div class='scroll-load-msg'>
			<img src='/assets/loading_max.gif' />正在加载...
		</div>
	</div>
</div>

<script type="text/template" id="category_product-preview-template">
	<%= render "shop_products/preview_template" %>
</script>

<script type="text/javascript">
	$(function(){
		new ActivitiesView({
			el: '#category_products',
			wrap_id: 'category_products'
		})
		new LoadCategoryProducts({
			el: $('.category_products_panel'),
			fetch_url: "/catalog/<%= @catalog.id %>/products"
		})

        new ShopProductPreview({
            el: $("#category_products")
        })

        new CatalogChildrenView({
        	el: $("#activity-category"),
        	catalog_id: "<%= @catalog.id %>"
        })
	})
</script>
