<h1>Listing shop_products</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shop_products.each do |shop_product| %>
  <tr>
    <td><%= link_to 'Show', shop_product %></td>
    <td><%= link_to 'Edit', edit_shop_product_path(shop_product) %></td>
    <td><%= link_to 'Destroy', shop_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shop product', new_shop_product_path %>
