<!-- <p id="notice"><%= notice %></p>
<p>
<%#= image_tag @activity.photos.avatar, :style => "width:300px;height:240px;" %>
</p> -->
<!-- <%#= link_to 'Edit', edit_activity_path(@activity) %> | -->
<%= render "activities/#{@activity.activity_type}/show", activity: @activity, product_item: @product_item %>

<script type="text/javascript">
    // $(function(){
    //   new ActivityView({
    //     el: $(".content"),
    //     login: '<%= current_user.login %>'
    //   })
    // })
	$(function(){
		new ActivityBind({
			model: (new ActivityModel({ id: "<%=@activity.id %>", shop_id: "<%= @activity.shop_id %>" })),
			el: $(".content")
		})
	})
</script>
