<div class="container-fluid offset2 span8">
  <div class='row-fluid'>
    <div class='span12'>
      <h2 class="page-title">买家信息验证</h2>
    </div>
  </div>
  <div class='row-fluid wrapper-box'>
    <%= simple_form_for @user_auth, 
            url: '/completing_people/authenticate_license', 
            html: { method: :put, class: "form-horizontal" } do |f| %>
      <%= f.input :user_name, label: '个人昵称' %>
      <%= f.input :user_photo, label: '个人头像', as: 'file' %>
      <%= f.input :true_name, label: '真实姓名' %>
      <%= f.input :user_id_number, label: '身份证号' %>
      <%= f.input :user_contact, label: '联系电话' %>
      <div class="control-group">
        <div class="controls">
          <button type="submit" class="btn">下一步</button>
          <%= link_to '跳过', next_wizard_path, :class => 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>
