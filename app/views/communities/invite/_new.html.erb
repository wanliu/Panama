<% if @circle.is_manage?(current_user) %>
<div>
  <form>
    <div class='row-fluid'>
      <div class='span2 title'>
        <img class='' src="<%=current_user.photos.icon %>" />
        <%=current_user.login %>
      </div>
      <div class='span10'>
        <textarea placeholder='留言对方' name='body' class='span12 body'></textarea>
      </div>
    </div>
    <div class='row-fluid user'>
      <div class='span2 title'>
        用户
      </div>
      <div class='span10'>
        <input type="text" name='login' class='login' autocomplete = "off"/>
      </div>
    </div>
    <div>
      <input type='submit' value="邀请" class='btn pull-right btn-primary' />
    </div>
  </form>
</div>
<% end %>