<div id="page" >
	<div class="row-fluid">
		<div class="span9">
			<div class="row-fluid">
				<%#= render_widget :search, :display, :content_query, nil, :class => 'span12' %>
			</div>
		</div>
		<div class="span3">
			<div class="row-fluid">
				<%= link_to t(:create), "#/new", :class => 'btn', :id => :new_order %>
			</div>
		</div>
	</div>
	<div class="row-fluid">
		<%= render_widget :table, :display, @products, nil, 
			:heads => [:name, :price, :amount, :bonus, :util],
			:head_options => { 
				:class => %w(table table-striped table-bordered table-hover)
			},
			:add_column => [ {:util => proc do |row|
				content_tag :div, :class => 'btn-group' do 
					link_to(icon(:edit), "#/#{row[:_id]}/edit", :class => 'btn utils edit') +
					link_to(icon(:remove), "#", :'data-delete-url' => "#{row[:_id]}", :class => 'btn utils delete')
				end
			end }],
			:hide_column => [:_id]
		%>

	</div>
</div>
