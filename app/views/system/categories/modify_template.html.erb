<%= active_admin_form_for @content.template, :url => update_template_system_category_path, :method => :put do |f| %>
  <% f.inputs "Template" do %>
  	<%= f.input :name, :as => :hidden %>
    <%= f.input :data, :as => :text %>
  <% end %>
  <%= f.actions %>
<% end %>
