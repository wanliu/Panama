<%= render :partial => "table", :locals => { :items => @property.items } %>

<% @item = PropertyItem.new %>

<%= active_admin_form_for @item, :url => append_item_system_property_path(@property) do |f| %>
  <% f.inputs "值" do %>
    <%= f.input :value %>
  <% end %>
  <%= f.buttons %>
<% end %>
<SCRIPT type='text/javascript'>
	function items_delete(opt){
		$.get("/propertie_item/"+opt,
    	    {p: ""},
    	    function(data){
    	    	if(data.success){ window.location.reload() }
    	    });
	}
</SCRIPT>