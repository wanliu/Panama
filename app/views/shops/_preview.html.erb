<div class="shop_product" id="<%=shop_product.id%>" style="display: inline-block">
	<div class='out-box'>
		<div class='in-box'>
			<%= image_tag shop_product.photos.avatar, :class => "preview a-#{shop_product.id}" %>
 			<div class='summary'>
				<h4 class="title"><%= shop_product.product.description %></h4>
				<p><%= shop_product.product.description %></p>
 			</div>
 		</div>
	</div>
</div>